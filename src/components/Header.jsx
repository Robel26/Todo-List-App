// Here is the Header component that is used to display the title of the to-do list.
// It returns a h1 element with the text "📝 To-Do List".
// The h1 element is styled using index CSS.

const Header = () => {
  return <h1>📝 To-Do List</h1>;
};

export default Header;
